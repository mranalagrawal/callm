mutation cartNoteUpdate($cartId: ID!, $note: String) {
  cartNoteUpdate(cartId: $cartId, note: $note) {
    cart {
      # Checkout fields
      note
    }
    userErrors {
      field
      message
    }
  }
}
